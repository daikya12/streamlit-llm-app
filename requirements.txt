# --- Minimal, production-ready for this app ---

# Web UI
streamlit>=1.41.1

# LangChain + OpenAI 連携
langchain>=0.3.0
langchain-openai>=0.2.0
openai>=1.30.0,<2.0.0

# Tokenizer（内部で必要になることが多い）
tiktoken>=0.7.0

# .env 読み込み
python-dotenv>=1.0.0